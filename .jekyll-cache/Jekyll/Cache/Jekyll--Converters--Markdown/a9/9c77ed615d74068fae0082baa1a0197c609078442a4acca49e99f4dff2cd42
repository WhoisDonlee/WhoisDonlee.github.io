I"W<h1 id="nextflow-vc-pipeline">NextFlow-VC-pipeline</h1>

<p>NextFlow-VC-pipeline is a pipeline to detect structural variants. The pipeline is based on the <a href="https://github.com/hartwigmedical/pipeline5">hartwig medical foundation pipeline</a>.
It’s built in NextFlow in combination with Singularity.</p>

<h2 id="installation">Installation</h2>

<p>The installation requires Singularity (3.7.1 tested) and NextFlow (20.10 tested).</p>

<p>Singularity can be installed using the <a href="https://sylabs.io/guides/3.7/user-guide/quick_start.html#quick-installation-steps">Singularity documentation</a>.</p>

<p>NextFlow can be installed using conda or using the <a href="https://www.nextflow.io/docs/latest/getstarted.html#installation">NextFlow documentation</a>.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda <span class="nb">install</span> <span class="nt">-c</span> bioconda nextflow
</code></pre></div></div>

<p>Building the Singularity container. Documentation recommends using sudo, however it’s not required.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>singularity build NextFlow-VC.sif NextFlow-VC.def
</code></pre></div></div>

<h2 id="usage">Usage</h2>

<p>First change the nextflow.config file to match the input file paths.
Following example is for Paired-End samples named WGS_Norm_Lane#_R#.fastq.gz</p>
<pre><code class="language-NextFlow">params {
    // Input parameters
    sampleBaseName = "WGS_Norm"

    refgen = "$HOME/data/refgen/rg6-17.fa.gz"
    reads = "$HOME/data/sample/WGS_Norm/${params.sampleBaseName}*{1,2}.fastq.gz"
    outdir = "$HOME/data/output/"
    
    threads = 8 // max cpus available
}
</code></pre>

<p>To run the pipeline:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nextflow <span class="nt">-log</span> logs/nf.log run pipeline.nf
</code></pre></div></div>

<p><a href="./info.md">Link to default template info</a>.
<a href="./another-page.html">Link to another page</a>.</p>
:ET